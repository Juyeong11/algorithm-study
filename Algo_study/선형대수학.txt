20.03.04
선형대수의 입장에서 곱셈은 특정좌표계에서 외관에 대한 성질에 불과하다.

덧셈과 정수배가 정의된 세계를 선형공간이라 부른다.

길이, 각도가 정의되어 있는 것은 내적 공간이다.

차원 = 기저 벡터의 개수 = 좌표의 성분 수

20.03.05
사상≒변환    행렬은 사상이다.

사상은 함수의 개념을 추상화 한것

행렬을 곱하다 -> 순수한 관계

e1 1 을 1  로    0 을 -0.3 에 이동 시킨다.
   0   0.7	 1     0.3

m*n행렬 A는 n차원 공간을 m차원 공간에 옮기는 사상을 나타낸다.
사상이 같다면 행렬도 같다 -> 같은 크기의 행렬 A,B가 임의의 백터 x에 대해 
			     항상 Ax=Bx라면 A=B이다.

행렬의 곱 = 사상의 합성

A하고, B한다 곱BA

대각행렬 -> 축에 따른 신축

역행렬 = 역 사상

이동점y를 갖고 원래의 점 x를 구한다. -> 눌려버린 행렬에는 역행렬이 존재하지 않는다.

A역행렬의 역행렬 = A AB의 역행렬은 B역행열A역행렬 A^k^-1=A^-1^k

XX^-1 = I

역행렬이 존재한다면 좌표의 변환